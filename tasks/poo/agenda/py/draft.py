class Fone:
    def__init__(self,id:str, number:str):
        self.id = id
        self.number = number
    
class Contact:
    def __init__(self, name: str):

    def addFone(self,id, number):
        f = Fone (id, number)

def main():
    agenda : Agenda()
        while True:
        try:
        line = input.strip()
            except EOFError:
        break

        if line == " ":]
        continue


main()